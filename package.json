{
  "name": "safe-window",
  "description": "A utility for safely getting properties from the window object in projects that render on both the server and the client.",
  "version": "0.0.3",
  "repository": {
    "type" : "git",
    "url" : "https://github.com/chardos/safe-window.git"
  },
  "types": "src/types.d.ts",
  "main": "src/index.js",
  "scripts": {
    "test": "jest --watchAll"
  },
  "author": "",
  "license": "ISC",
  "jest": {
    "testEnvironment": "node"
  },
  "devDependencies": {
    "jest": "23.6.0"
  }
}
